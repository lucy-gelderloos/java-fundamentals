# Java Fundamentals

## Main

**pluralize** takes in a string and number and returns a string with the pluralized input.

**flipNHeads** simulates flipping a coin until a specified number of "heads" are flipped in a row, and prints each flip result as well as the total number of flips to the console.

**clock** prints the current time to the console, updating each second, until manually terminated.
